[package]
name = "recipe_calculator_server"
version = "0.1.0"
authors = ["blazern <blazer007@yandex.ru>"]

[dependencies]
docopt = "0.8"
serde = "1.0" # needed for docopt + derive(Deserialize)
serde_derive = "1.0" # needed for docopt + derive(Deserialize)
serde_json = "1.0"
time = "0.1"
